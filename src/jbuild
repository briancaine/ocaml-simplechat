;; -*- mode: lisp -*-

(jbuild_version 1)

(executables
 ((names (sc_client sc_server))
  (libraries
   (core conduit-lwt-unix lwt bin_prot ANSITerminal))
  (preprocess
   (pps
    (ppx_deriving
     ppx_sexp_conv
     ppx_bin_prot
     lwt.ppx)))))
